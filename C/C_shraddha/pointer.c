#include <stdio.h> 

int main(){

    // int age = 22;
    // int *ptr = &age;
    // int _age = *ptr;

    // printf("%d", _age);
    //         // declaring pointers: int*ptr, char*ptr float*ptr




    char name = 'szrthk'; // this is called pointers pointers are used to store variable of addres, and today we arezzz
    char *ptr = &name;
    char _name = *ptr;

    printf("%c", _name);
    return 0;
}